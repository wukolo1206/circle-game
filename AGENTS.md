# AGENTS.md — circle-game

## 開始前必讀

1. 讀本資料夾 `CLAUDE.md`。
2. 讀本資料夾 `handoff.md`。
3. 遵守上層 `D:\備課ai\三下數學\AGENTS.md` 與全域規則。

## 專案定位

- 第六單元「圓」互動工具。
- 技術：純 HTML + Canvas。
- 部署：GitHub Pages，網址 `https://wukolo1206.github.io/circle-game/`。

## 修改規則

- 修改既有 HTML 前，先覆蓋同名 `.bak`。
- 保持純靜態，不新增 build 流程。
- 觸控事件與 Canvas 狀態不要隨意重構。

## 結束時

更新 `CLAUDE.md` frontmatter、`handoff.md`，有功能完成則更新 `CHANGELOG.md`，有踩坑則更新 `PITFALLS.md`，有架構選擇則更新 `DECISIONS.md`。
